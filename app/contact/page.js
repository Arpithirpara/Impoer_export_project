import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us | AgriHarvest",
  description:
    "Get in touch with AgriHarvest for agricultural export inquiries, bulk orders, and partnership opportunities.",
};

export default function ContactPage() {
  return <ContactClient />;
}